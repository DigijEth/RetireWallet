<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/balanceText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Balance: 0.0 ETH"
        android:textSize="24sp"
        android:layout_alignParentTop="true"
        android:paddingBottom="24dp"/>

    <Button
        android:id="@+id/depositButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Deposit"
        android:layout_below="@id/balanceText"
        android:layout_marginTop="16dp"/>

    <Button
        android:id="@+id/withdrawButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Withdraw"
        android:layout_below="@id/depositButton"
        android:layout_marginTop="16dp"/>

    <Button
        android:id="@+id/viewHardshipCodesButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="View Hardship Codes"
        android:layout_below="@id/withdrawButton"
        android:layout_marginTop="16dp"/>

</RelativeLayout>
